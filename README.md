# Student Management System

A simple and efficient command-line application built in **C** to manage student records.

## 🚀 Features
* **Add student details** Create new records with ID, name, and CGPA.
* **Modify student details** Modify the existing records.
* **Display all student details** Display a list of all students currently in the system.
* **Search for student details** Find specific students quickly using their ID.
* **Total number student registered** Display number of students currently in the system.
* **End task** Finishes the program.

## 🛠️ How to Run
To compile and run this project on your computer, follow these steps:

1. Open your terminal or command prompt.
2. Navigate to the folder containing `main.c`.
3. Compile the code using GCC:
   ```bash
   gcc main.c -o student_system
